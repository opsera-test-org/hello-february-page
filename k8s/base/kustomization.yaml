apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

# Common labels (safe - doesn't modify selectors)
labels:
  - includeSelectors: false
    pairs:
      managed-by: opsera-c2c
      tenant: opsera
